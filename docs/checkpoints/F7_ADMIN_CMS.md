# Cierre F7 — CMS administrativo

Fecha: 2026-09-06. Implementación, pruebas locales y comprobación remota terminadas. Sin push, PR ni merge. [Guía técnica y operaciones](../ADMIN_CMS.md).

1. **SHA base:** `5f0e42910983dfbf14c919769d2c057c7811625e`, origin/main sincronizado y árbol limpio antes de crear la rama. Incluye F10/PR #12 y todas las fases/checkpoints anteriores aprobados. No se reutilizó su worktree.
2. **Rama/worktree:** `feat/f7-admin-cms`; `C:/laragon/www/portfolio/.worktrees/f7-admin-cms`. Trabajo local aislado; main permanece en la base aprobada.
3. **Rutas:** 25 páginas físicas: `/admin/`, `login/`, `reset-password/`, `projects/`, `projects/new/`, `projects/edit/`, `posts/`, `posts/new/`, `posts/edit/`, `experience/`, `technologies/`, `specialties/`, `principles/`, `impact/`, `media/`, `documents/`, `messages/`, `analytics/`, `contact/`, `social/`, `seo/`, `settings/`, `builds/`, `categories/` y `tags/`. Todas usan la base del repositorio; edición por `?id=UUID`, sin SSR.
4. **AdminLayout:** shell estático con sidebar, header, usuario/logout, estado de sesión y contenido progresivo. Reutiliza fuentes/tokens F2 con menos glow/blur, H1 único y noindex,nofollow. Módulos privados se cargan después de Auth; imports por ruta.
5. **Login:** email/password mediante SDK oficial, sin registro público. Errores genéricos; exige usuario real y perfil owner activo leído bajo RLS. Login válido/inválido probado con Auth local real. En remoto se verificó sesión real del owner por enlace administrativo de un solo uso intercambiado en memoria, sin correo ni contraseña compartida.
6. **Logout:** revoca la sesión actual, limpia DOM/estado privado y vuelve a login. Se probaron reload y navegación posterior sin datos privados cacheados.
7. **Recovery:** solicitud, PKCE, callback físico, código retirado de URL, validación owner, nueva contraseña y errores de enlace. Flujo completo probado con Auth/Mailpit local y contraseña aleatoria de fixture. No se cambió la contraseña remota. Requiere el mismo navegador que inició la solicitud.
8. **Guard owner:** estados loading/anonymous/denied/owner/error. `auth.getUser()` → `admin_profiles` → owner activo. F6 oculta perfiles ausentes e inactivos; ambos se presentan como denegados para conservar esa frontera. Metadata, email y flags de almacenamiento no autorizan.
9. **Deep links:** UUID validado antes de consultar; registro inexistente muestra estado seguro. Sin sesión vuelve a login y retorna únicamente a rutas Admin permitidas. Se rechazan destinos externos, rutas no permitidas y parámetros ajenos.
10. **Dashboard:** conteos reales de proyectos/posts publicados y drafts, mensajes nuevos, resumen Analytics, último build y accesos rápidos. Estados vacíos y reintento; sin números ficticios.
11. **Projects CRUD:** listado paginado de 20, búsqueda/filtros/orden, draft nuevo, editar, featured/publicación editorial y eliminación confirmada. Persistencia owner-only y detección de revisión obsoleta.
12. **Project editor:** campos reales generales, case study, estado/año, links, SEO, media, tecnologías, funcionalidades, métricas, desafíos y galería. Secciones opcionales. Relaciones y padre se guardan en una transacción; no nuevas columnas de contenido.
13. **Autosave:** debounce de 1,5 s y cola serial; sin cambios/pendiente/guardando/guardado/error/conflicto. Una respuesta antigua no marca cambios nuevos como guardados. Fallos conservan texto y permiten reintentar.
14. **Publish state:** Guardar persiste; Publicar valida y cambia estado editorial, después solicita el contrato `publish-site`. Sin infraestructura F11 muestra contenido público guardado y rebuild pendiente; nunca confirma una publicación estática inexistente.
15. **Blog CRUD:** listado, crear/editar, fechas, excerpt, Markdown, hero, SEO y estados exactos draft/published/archived. Sin rutas dinámicas server-side.
16. **Markdown editor/preview:** utiliza exactamente `renderMarkdown()` de F4, parser/sanitizer/TOC compartidos, code blocks y reading time. Preview cargada bajo demanda; imágenes autorizadas por F8 con Blob URLs efímeras. Pruebas XSS no ejecutan script, SVG ni handlers.
17. **Categorías/tags:** CRUD propio, slugs validados y relaciones múltiples del post guardadas atómicamente. No se alteraron las categorías productivas del seed.
18. **Experiencia:** CRUD de cargo/organización/períodos/resumen/visibilidad/orden, highlights y relaciones con proyectos/tecnologías mediante `save_experience`.
19. **Tecnologías:** nombre, slug, categoría, icono versionado/media, featured, visibilidad y orden. Sin porcentajes de dominio.
20. **Especialidades:** título, descripción, icono, visibilidad y orden; formularios tipados y eliminación confirmada.
21. **Principios:** administración de los registros existentes y su orden; ninguna modificación permanente del seed durante pruebas.
22. **Impacto:** valores textuales, etiqueta, descripción/visibilidad/orden según esquema; no convierte métricas como `24/7` en numeric ni inventa resultados.
23. **Media library:** integra F8 real: buscar/filtrar, upload privado, metadata, alt/decorative, preview, publicación explícita, usos, reemplazo y borrado seguro. Mantiene MIME/extensión/bytes/10 MiB y rutas UUID inmutables.
24. **MediaPicker:** selector accesible de media existente o upload privado; búsqueda, preview, alt y estado, cancelar/Escape/retorno de foco. Devuelve ID editorial; no persiste signed URL.
25. **Documentos/CV:** PDF validado, registro/título, publicación mediante copia F8, verificación, activar/desactivar y eliminación del registro conservando bytes. `activate_cv` mantiene un único CV activo y desactiva el anterior. No se subió CV real.
26. **Mensajes:** listado paginado, filtro, detalle como texto escapado y estados new/read/replied/archived. Mailto abre el cliente del owner; copiar/marcar respondido no simula envío. No botón de borrado libre ni proveedor de email nuevo.
27. **Contact settings:** edición del singleton existente: email, WhatsApp, mensaje por defecto y controles de visibilidad/formulario/CV aprobados. El próximo build recoge los cambios.
28. **Social links:** CRUD, búsqueda, URLs HTTPS sin credenciales, visibilidad y orden. Se rechazan protocolos inseguros.
29. **Site settings:** campos existentes de identidad, claim, descripciones/perfil, disponibilidad, timezone y configuración pública. Guardado con revisión de `updated_at`; no cache privada persistente.
30. **SEO:** campos globales y por contenido existentes: title/description, canonical, robots y OG/media. Validación de URL/base; no auditoría SEO F12 ni nuevos campos.
31. **Analytics:** consume exclusivamente DTO/RPC owner-only F10. Períodos 7/30/90/366 o personalizado acotado, vistas, sesiones aproximadas, conversiones, interacciones, tendencia SVG y tablas top. Sin raw events, nuevo tracking ni identificación de visitantes.
32. **Builds:** lectura de los últimos 20; queued/building/success/failed, fecha, commit, duración y error resumido. No dispatch productivo ni creación de builds ficticios.
33. **Auditoría:** conserva el contrato F6/F9 de `admin_activity`: lectura owner, escritura de servicios. No existía un trigger general de auditoría CRUD para reutilizar; no se abrió INSERT desde navegador ni se registran pulsaciones, mensajes completos o secretos.
34. **Validación/errores:** campos del schema, slug ASCII/lowercase, UUID, fechas, enums, URL, requeridos y límites. UNIQUE/FK/CHECK/RLS se traducen a mensajes comprensibles sin SQL/stack. PostgreSQL continúa siendo autoridad.
35. **Dirty/concurrencia:** aviso antes de abandonar cambios, estado conservado en memoria ante expiración, guardados seriales y revisión optimista. RPC bloquea padre y compara `updated_at`; cambios en hijos avanzan esa revisión. Conflicto devuelve PT409/HTTP 409 inmediatamente. No sobrescritura silenciosa ni versionado editorial independiente.
36. **Mobile:** sidebar colapsable, Escape/foco/body lock y formularios adaptables. Verificación 360/390/768/1024/1440/1920, sin overflow global. Acciones dejan de ser sticky en móvil para no cubrir campos.
37. **Accesibilidad:** labels/help/error, feedback persistente aria-live, dialogs nativos, tabs/teclado, foco, targets y tablas. Axe sin infracciones en los 27 recorridos principales autenticados a 390/768/1440; navegación/menú/selector/feedback probados con teclado. Esto no sustituye una auditoría manual completa con todos los lectores de pantalla.
38. **Unitarias:** 182 npm aprobadas, incluidas seis nuevas pruebas agrupadas Admin de guard, validación, rutas/UUID, errores, autosave/concurrencia y builds. Se conserva la cobertura F4 de Markdown/sanitización y F8/F10 de media/DTO.
39. **E2E:** 15 recorridos CMS aprobados contra Auth/PostgreSQL/Storage locales reales; incluye recovery PKCE completo, sesión expirada, CRUD, autosave/conflicto, relaciones, picker/PDF/CV, mensajes XSS, Analytics y settings. Suite pública: 197 aprobados y 136 omisiones deliberadas por breakpoint. Prueba visual repetida tras ajustes: aprobada.
40. **Adversariales:** metadata owner falsa, noowner/inactivo, UUID inválido/ajeno, REST/RPC directo, hijo de otro padre, payload no permitido, rollback atómico, revisión obsoleta, XSS Markdown/mensaje, URL insegura y sesión inválida. Sin depender de protecciones de interfaz.
41. **SQL/RLS/Auth:** 745 SQL (31 nuevas F7), lint y tipos aprobados. 107 Auth/RLS HTTP, 66 Storage/media, 99 Edge, 79 Edge HTTP, 49 Analytics HTTP y nueve comprobaciones del formulario local. Upgrade 019–022 con padre/hijo existente →023 y reconstrucción limpia 019→023+seed aprobados. 35 tablas RLS, 138 policies sin ampliación de grants de tabla.
42. **Visual review:** 30 capturas realmente inspeccionadas: Dashboard, Projects list/editor, Blog list/Post editor, Media, Messages, Analytics, Settings y Login a 390/768/1440. Se redujo el solapamiento de la barra de acciones móvil y se corrigió un token de espaciado; capturas finales repetidas. [Manifest de capturas](f7/visual-review.json); PNG locales en `.tools/admin-screenshots/` ignorado.
43. **Build público:** snapshot remoto real con publishable key, sin JWT owner ni server key. Build posterior a limpieza genera 31 páginas: seis públicas existentes y 25 shells Admin. Cero rutas de proyectos/posts ficticios; estados vacíos públicos conservados. No-JS/reduced motion públicos siguen aprobados.
44. **Secretos:** static/noindex/H1 en 25 rutas; 371 artefactos de texto inspeccionados y 381 artefactos en check:secrets. Build de prueba comprobó 15 canaries privados locales; smoke remoto comprobó identidades/fixtures reales sin revelar valores. No mensajes, Analytics raw, owner UUID, password, JWT, refresh token o clave privilegiada en HTML/JS generado.
45. **Migración F7:** `20260906002300_admin_editorial_transactions.sql`, sin alterar 019–022. Tres RPC SECURITY INVOKER owner-only (`save_project`, `save_post`, `save_experience`) y helper privado invoker con diez triggers de revisión del padre. Sin tablas/columnas de contenido, policies, grants de tablas ni nuevos SECURITY DEFINER.
46. **Cambios remotos:** solo 023 a las 23:22 UTC, tras [36 controles locales](f7/local-gate.json), backup y dry-run exclusivo. Proyecto portfolio-alonso/sa-east-1, PG17.6, CLI2.116.0 autenticada/enlazada. Historial019–023, catálogo/tipos iguales, dry-run posterior vacío y Automatic RLS activo. 34 smoke checks reales aprobados, incluido guardar desde navegador y leer Analytics con JWT owner. Auth/owner/configuración, buckets, secrets, Edge y tracking no cambiaron.
47. **Fixtures:** eliminados todos los registros/objetos/usuario noowner temporales. Estado remoto final: un usuario Auth/owner original, cero proyectos/posts/experiencias/tecnologías/media/objetos/mensajes/eventos/builds de testing. Snapshot público no expuso drafts durante el smoke. Local queda con seed y cero usuarios/media de prueba. [Comprobación posterior](f7/final-state.json).
48. **Archivos:** [inventario completo](f7/changed-files.txt). Layout/rutas Admin, módulos/repositorios tipados, integración del SDK, CSS F2, 023/tipos/SQL, tests y scripts operacionales, documentación. No cambios de diseño F3/F4 ni de intake F9/F10.
49. **Dependencias:** ninguna nueva; package-lock intacto. SDK, Markdown/sanitizer, Sharp/PDF, Playwright y herramientas ya fijadas. Sin React/Vue/Svelte ni biblioteca de gráficas.
50. **Commits:** `27b481f` transacciones/RLS, `2a0642d` whitespace de migración, `25dbb3c` servicios tipados, `88199d1` shell/módulos, `3491b1c` pruebas y recorridos, `9940f7d` contratos/operación. El cierre documental se registra con `docs: record F7 cms validation and remote deployment`; consultar `git log origin/main..HEAD --oneline` para el SHA del propio cierre. Sin reescritura de historial ni push/PR/merge.
51. **Desviaciones:** noowner/inactivo se agrupan visualmente porque RLS oculta ambos perfiles. Se añadió la transacción editorial necesaria para evitar guardados parciales y pérdida de relaciones. El conflicto usa PT409 tras demostrar por HTTP que 40001 causaba reintentos/timeout de PostgREST. Dos ajustes de harness existentes: crear el directorio del reporte Analytics en worktree nuevo y esperar correctamente la salida de Astro en el test de contacto. No cambiaron sus funciones ni contratos. Login password/recovery remoto no automatizados con la contraseña definitiva; sesión real comprobada por el canal administrativo seguro ya aprobado.
52. **Riesgos/pendientes:** cambios en registros ya públicos pueden ser recogidos por el próximo build; V1 no mantiene versiones separadas de borrador. Borradores no guardados sobreviven a expiración solo en la pestaña, no al cierre deliberado. PKCE requiere mismo navegador. El backup lógico nuevo no se restauró independientemente y no incluye bytes Storage; recuperación gestionada Free no disponible según checkpoint aprobado. E2E/axe usan Chromium; no se declara auditoría manual completa de lectores de pantalla ni todos los navegadores.
53. **Estado exacto F11:** `publish-site`/workflow/callback productivos siguen sin configurar. El CMS guarda datos y muestra rebuild pendiente; no se creó PAT, workflow, repository_dispatch real ni publicación GitHub Pages.
54. **Límite:** F10B/F11/F12/F13/F14 NO iniciadas. F7 se detiene para revisión; branch local con commits y árbol limpio, sin push, PR ni merge automático.

## Evidencia operativa

[Preflight](f7/remote-preflight.json), [backup y límites](f7/backup-manifest.json), [upgrade local](f7/upgrade.json), [puerta del push](f7/deploy-gate.json), [resultado 023](f7/push.json), [catálogo/tipos](f7/post-deploy.json), [smoke remoto](f7/remote-smoke.json), [build posterior](f7/post-build.json) y [validaciones finales](f7/validation-summary.json).

El catálogo cotejado incluye 405 columnas, 257 constraints, 101 índices, 55 triggers, 32 funciones propias, cuatro vistas, 39 relaciones (35 tablas + cuatro vistas), 138 policies, grants de columnas y dos jobs. Los cuatro SECURITY DEFINER propios permanecen idénticos; el helper remoto `rls_auto_enable` es configuración de plataforma preexistente. 023 no modifica la estructura de Auth/Storage ni la FK UUID de F8.

Los backups privados están en `%LOCALAPPDATA%/portfolio-alonso/backups/20260906-f7-admin`, fuera de Git y con ACL restringida. Solo se versionan nombres, tamaños, hashes y límites. Las evidencias no incluyen credenciales, valores de configuración pública, email/UUID del owner ni contenido de mensajes.
